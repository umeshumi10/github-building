name: 01-Building Blocks
on: 
  push:
  workflow_dispatch:
jobs:
  echo-hello:
   runs-on: ubuntu-latest
   steps:
    - name: Say Hello
      run: echo "Hello"
  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: Failed Step
        run: |
           ehco " I will Fail"
           exit 1
      - name: Say goodbye
        run: echo "GoodBye"
